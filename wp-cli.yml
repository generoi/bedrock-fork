path: web/wp

# Aliases to other WordPress installs (e.g. `wp @staging rewrite flush`)
# An alias can include 'user', 'url', 'path', 'ssh', or 'http'
# @todo: dev alias requires password to be entered every time
@dev:
  ssh: vagrant@<example-project>.dev
  path: /var/www/wordpress/web/wp
  url: <example-project>.dev

@staging:
  ssh: deploy@minasanor.genero.fi
  path: /var/www/staging/<example-project>/current/web/wp
  url: <example-project>.web.staging.minasanor.genero.fi

# @todo: make alias for production
